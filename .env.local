# Private URL used only by Next.js server routes. Do not expose JWT keys to the browser.
BACKEND_URL=http://127.0.0.1:8000

# Adapter key registered in backend (required when creating API suppliers in production).
# NEXT_PUBLIC_DEFAULT_PROVIDER_ADAPTER=
